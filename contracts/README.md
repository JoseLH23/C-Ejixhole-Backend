# Contratos de API

Esta carpeta contiene snapshots generados desde FastAPI para sincronizar clientes.

El contrato se genera desde `app.openapi()` y los tipos se producen con una versión fijada de `openapi-typescript`. Los archivos comprometidos deben coincidir exactamente con la evidencia de CI.
